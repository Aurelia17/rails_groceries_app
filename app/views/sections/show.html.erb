<h1>Sections#show</h1>
<p>Find me in app/views/sections/show.html.erb</p>
<% @products.each do |product| %>
  <% if raise @section.id == product.section_id %>
    <h1><%= product.title %></h1>
  <% end %>
<% end %>
